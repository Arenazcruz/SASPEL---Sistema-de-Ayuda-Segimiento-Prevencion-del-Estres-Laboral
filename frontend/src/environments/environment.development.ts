/**
 * Base API para desarrollo local, seleccionada por fileReplacements de angular.json. Mantener
 * origen/puerto alineados con Django y CORS al cambiar el entorno.
 */
export const environment = { apiUrl: 'http://127.0.0.1:8000/api' };
