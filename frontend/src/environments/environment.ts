/**
 * Base API de compilación normal. API_URL la comparte con servicios e interceptor; para
 * despliegues revisar este valor y la configuración CORS del backend.
 */
export const environment = { apiUrl: '/api' };
